#android_animation_engine
==============

android原生的动画库限制太大了，不能产生更多的效果，
所以就开发了此动画引擎库，它能帮助你实现很多原生动画没法做到的事情。

目前此动画库基于canvas进行渲染，效率不能支持很高，
现在在完善动画库结构，等比较成熟的时候，会考虑将动画的渲染交给GPU进行渲染加速，
这样可以支持非常复杂的动画，比如流畅的支持复杂的游戏引擎的粒子效果等。